const { join } = require('path')
// eslint-disable-next-line import/no-extraneous-dependencies
const {
  eslint: {
    base: { getBaseESLint },
  },
} = require('@unikum/base-nodejs')

module.exports = {
  ...getBaseJest({ pathFileTSConfig: join(__dirname, 'tsconfig.eslint.json') }),
  ignorePatterns: ['build/**/*', 'dist/**/*'],
}
